# Compiled binary
webrtc-vnc
webrtc-vnc.exe
webrtc-vnc-darwin-arm64
webrtc-vnc-darwin-amd64

# Local ffmpeg copy with cap_sys_admin (created by setup.sh)
ffmpeg-cap

# Native capture+encode helpers (built per-OS, see cmd/<helper>/)
nvfbc_capture
nvfbc_nvenc
screenkit_vt
dxgi_mf
dxgi_mf.exe

# cloudflared binary built from third_party/cloudflared submodule
cloudflared-bin

# Go build cache
*.test
*.out

# Editor / OS
.DS_Store
*.swp
*.swo
*~
